# Historia #2.2: Cancelación de suscripción a fondo de inversión

**Estado:** Analizado (Arquitecto)

---

## Historia de Usuario

**Como** cliente de la plataforma BTG Pactual,  
**Quiero** cancelar la suscripción a un fondo de inversión al que estoy vinculado,  
**Para** recuperar el monto invertido en mi saldo disponible y poder reasignarlo a otros fondos o mantenerlo como saldo libre.

## Descripción

El cliente puede cancelar cualquiera de sus suscripciones activas a fondos de inversión. La cancelación se realiza identificando la suscripción por su ID único. Al cancelar, el monto que fue invertido se devuelve íntegramente al saldo del cliente. Solo se pueden cancelar suscripciones con estado ACTIVO. El cliente tiene libertad de cancelar todas sus suscripciones si así lo desea, sin restricción de mantener al menos una. Tras la cancelación, el cliente puede re-suscribirse al mismo fondo en el futuro (definido en HU 2.1).

---

## Criterios de Aceptación

### Escenario 1: Cancelación exitosa de suscripción activa

- **Dado** que un cliente tiene una suscripción con estado ACTIVO a un fondo
- **Cuando** solicita cancelar dicha suscripción identificándola por su ID
- **Entonces** el estado de la suscripción cambia a CANCELADO, el monto invertido se devuelve al saldo del cliente, y se retorna la confirmación de la cancelación con los datos de la operación

### Escenario 2: Intento de cancelar suscripción no activa

- **Dado** que un cliente tiene una suscripción con estado CANCELADO o la suscripción no existe
- **Cuando** solicita cancelar dicha suscripción
- **Entonces** se rechaza la operación con el mensaje "No se puede cancelar el \<nombre del fondo\> ya que no esta activo dentro de su portafolio"

### Escenario 3: Verificación de devolución de saldo

- **Dado** que un cliente con saldo de COP $425.000 tiene una suscripción ACTIVA por COP $75.000 al fondo FPV_BTG_PACTUAL_RECAUDADORA
- **Cuando** cancela dicha suscripción
- **Entonces** el saldo del cliente queda en COP $500.000 (425.000 + 75.000 devueltos)

### Escenario 4: Cancelación de todas las suscripciones

- **Dado** que un cliente tiene múltiples suscripciones activas
- **Cuando** cancela todas sus suscripciones una por una
- **Entonces** todas las suscripciones quedan con estado CANCELADO, todo el monto invertido se devuelve al saldo, y el cliente puede quedar sin suscripciones activas (sin restricción)

### Escenario 5: Trazabilidad del registro cancelado

- **Dado** que un cliente cancela una suscripción
- **Cuando** se procesa la cancelación
- **Entonces** el registro de la suscripción se conserva en el sistema con estado CANCELADO (no se elimina), manteniendo el historial completo de la operación

---

## Información Recopilada

### Usuario y Contexto

- **Tipo de usuario:** Cliente registrado y autenticado en la plataforma (ver HU 1.1 y HU 1.2)
- **Permisos requeridos:** Rol CLIENTE — solo puede cancelar sus propias suscripciones (ver HU 1.3, aislamiento de datos)
- **Valor de negocio:** Permitir al cliente revertir inversiones y recuperar su dinero de manera autónoma, reforzando la confianza en la plataforma de autogestión

### Reglas de Negocio

- La cancelación se identifica por el ID único de la suscripción
- Solo se pueden cancelar suscripciones con estado ACTIVO
- Al cancelar, se devuelve el monto completo que fue invertido al saldo del cliente
- El registro de la suscripción se conserva con estado CANCELADO (nunca se elimina)
- El cliente puede cancelar libremente todas sus suscripciones (sin restricción de mantener al menos una)
- Tras cancelar, el cliente puede re-suscribirse al mismo fondo (ver HU 2.1, escenario 5)
- Manejo adecuado de excepciones con mensajes claros

### Catálogo de Excepciones

| Caso | Mensaje |
|---|---|
| Suscripción no activa o inexistente | "No se puede cancelar el \<nombre del fondo\> ya que no esta activo dentro de su portafolio" |

### Sistemas Externos

No aplica — la cancelación es un proceso completamente interno.

---

## Contexto y Referencias

**Arquitectura:** Arquitectura Hexagonal + CQRS + Mediador según [ARQUITECTURA.md](../../ARQUITECTURA.md). La cancelación requiere un nuevo CancelacionComando + CancelacionManejador en la capa de aplicación, extensión de IServicioSuscripcion con método cancelar(), y extensión de PuertoRepositorioSuscripcion con buscarPorId().

**Historias relacionadas:** HU 2.1 (suscripción que genera las suscripciones a cancelar), HU 2.3 (listado para identificar cuál cancelar)

**Historias hermanas (Slicing):** HU 2.1 (Suscripción), HU 2.2 (esta), HU 2.3 (Consulta vigentes), HU 2.4 (Notificación)

**Lecciones aprendidas:**
- La entidad Suscripcion ya tiene el enum Estado (ACTIVO, CANCELADO) pero no hay lógica que cambie el estado — esta historia la implementa
- La entidad Cliente necesita un método para abonar saldo (inverso de descontarSaldo) para la devolución
- El aislamiento de datos (HU 1.3) debe verificar que el cliente solo cancele sus propias suscripciones

---

## Definición de Terminado (Inicial)

- [ ] Cancelación cambia estado de ACTIVO a CANCELADO correctamente
- [ ] Monto invertido se devuelve al saldo del cliente
- [ ] Mensaje de error exacto cuando suscripción no está activa
- [ ] Registro se conserva (no se elimina)
- [ ] Manejo de excepciones implementado

---

## Análisis Arquitectónico

**Fecha de análisis:** 2026-03-12  
**Enfoque:** Exploratorio (B)  
**Arquitecto:** Agente Arquitecto Método Ceiba

### Hallazgos del Análisis

| Aspecto | Hallazgo |
|---|---|
| `Suscripcion.java` | Tiene `enum Estado {ACTIVO, CANCELADO}` y `estado` mutable, pero **NO hay método de dominio para cambiar estado** — se necesita `cancelar()` encapsulado |
| `Cliente.java` | Tiene `descontarSaldo()` pero **NO tiene `abonarSaldo()`** — requerido para devolución de saldo al cancelar |
| `IServicioSuscripcion` | Solo tiene `suscribir()` — necesita método `cancelar()` |
| `PuertoRepositorioSuscripcion` | Solo tiene `guardar()` y `existePorClienteIdYFondoId()` — necesita `buscarPorId()` |
| `AdaptadorSuscripcionEnMemoria` | Almacén ConcurrentHashMap<String, Suscripcion> por ID — `buscarPorId()` es trivial |
| Patrón existente | `FondoComando → FondoManejador → IServicioSuscripcion.suscribir()` — cancelación replica este patrón |
| Endpoint | ARQUITECTURA.md define `POST /api/suscripciones/{id}/cancelar` — ruta distinta a `/api/fondos` |

### Componentes (9 cambios)

#### Capa Dominio (3 modificaciones)

| # | Componente | Tipo | Ubicación |
|---|---|---|---|
| 1 | `Suscripcion.cancelar()` | Método nuevo | `domain/model/Suscripcion.java` |
| 2 | `Cliente.abonarSaldo(BigDecimal)` | Método nuevo | `domain/model/Cliente.java` |
| 3 | `PuertoRepositorioSuscripcion.buscarPorId()` | Método nuevo en puerto | `domain/port/out/PuertoRepositorioSuscripcion.java` |

**Detalle:**

1. **`Suscripcion.cancelar()`** → `BigDecimal` — Encapsula regla de negocio: valida `estado == ACTIVO`, lanza `ExcepcionDominio` con nombre del fondo si no activo. Cambia estado a `CANCELADO`. Retorna el `monto` para devolución.
2. **`Cliente.abonarSaldo(BigDecimal monto)`** — Operación inversa de `descontarSaldo()`. Suma monto al saldo. Sin validación adicional.
3. **`PuertoRepositorioSuscripcion.buscarPorId(String id)`** → `Optional<Suscripcion>` — Obtener suscripción a cancelar.

#### Capa Dominio — Servicio (1 modificación)

| # | Componente | Tipo | Ubicación |
|---|---|---|---|
| 4 | `IServicioSuscripcion.cancelar()` | Método nuevo | `domain/service/IServicioSuscripcion.java` |

**Detalle:**

4. **`IServicioSuscripcion.cancelar(String suscripcionId, String clienteId)`** → `Suscripcion` — Firma con clienteId para aislamiento de datos (HU 1.3).

#### Capa Infraestructura (2 modificaciones)

| # | Componente | Tipo | Ubicación |
|---|---|---|---|
| 5 | `ServicioSuscripcion.cancelar()` | Implementación nueva | `infrastructure/service/ServicioSuscripcion.java` |
| 6 | `AdaptadorSuscripcionEnMemoria.buscarPorId()` | Implementación nueva | `infrastructure/adapter/out/persistence/AdaptadorSuscripcionEnMemoria.java` |

**Detalle:**

5. **`ServicioSuscripcion.cancelar()`** — Orquestación:
   - `repositorioSuscripcion.buscarPorId(suscripcionId)` → si no existe → `ExcepcionDominio`
   - Valida `suscripcion.getClienteId().equals(clienteId)` → aislamiento de datos
   - `suscripcion.cancelar()` → regla encapsulada, retorna monto
   - `repositorioCliente.buscarPorId(clienteId)` → obtiene cliente
   - `cliente.abonarSaldo(monto)` → devuelve saldo
   - `repositorioCliente.guardar(cliente)` → persiste saldo actualizado
   - `repositorioSuscripcion.guardar(suscripcion)` → persiste estado CANCELADO
   - Retorna suscripción cancelada

6. **`AdaptadorSuscripcionEnMemoria.buscarPorId()`** → `Optional.ofNullable(almacen.get(id))` — Trivial dada la estructura del mapa.

#### Capa Aplicación (2 nuevos)

| # | Componente | Tipo | Ubicación |
|---|---|---|---|
| 7 | `CancelacionComando` | Record nuevo | `application/cancelacion/command/CancelacionComando.java` |
| 8 | `CancelacionManejador` | Clase nueva | `application/cancelacion/command/CancelacionManejador.java` |

**Detalle:**

7. **`CancelacionComando(String suscripcionId, String clienteId)`** — Record inmutable.
8. **`CancelacionManejador`** — Implementa `Manejador<CancelacionComando, FondoResultado>`. Reutiliza `FondoResultado` existente (mismos campos). Delega a `IServicioSuscripcion.cancelar()` y mapea a resultado.

#### Capa API (1 nuevo)

| # | Componente | Tipo | Ubicación |
|---|---|---|---|
| 9 | `ControladorSuscripcion` | Controlador nuevo | `api/controller/ControladorSuscripcion.java` |

**Detalle:**

9. **`POST /api/suscripciones/{id}/cancelar`** — Nuevo controlador `ControladorSuscripcion` (SRP: suscripciones ≠ fondos). Extrae suscripcionId del path y clienteId del request/JWT. Crea `CancelacionComando` y envía vía `Mediador`. Retorna `RespuestaSuscripcion` con HTTP 200.

### Estructura de Paquetes

```
application/
├── cancelacion/
│   └── command/
│       ├── CancelacionComando.java          ← NUEVO (record)
│       └── CancelacionManejador.java        ← NUEVO
├── fondo/
│   ├── command/
│   │   └── FondoResultado.java              (reutilizado por cancelación)
│   └── query/ ...

domain/
├── model/
│   ├── Cliente.java                         ← +abonarSaldo(BigDecimal)
│   └── Suscripcion.java                     ← +cancelar(): BigDecimal
├── port/out/
│   └── PuertoRepositorioSuscripcion.java    ← +buscarPorId(String): Optional<Suscripcion>
├── service/
│   └── IServicioSuscripcion.java            ← +cancelar(String, String): Suscripcion

infrastructure/
├── adapter/out/persistence/
│   └── AdaptadorSuscripcionEnMemoria.java   ← +buscarPorId()
├── service/
│   └── ServicioSuscripcion.java             ← +cancelar()

api/controller/
├── ControladorFondo.java                    (sin cambios)
└── ControladorSuscripcion.java              ← NUEVO
```

### Hitos de Implementación

| Hito | Componentes | Verificación |
|---|---|---|
| **H1** — Dominio: lógica de cancelación | `Suscripcion.cancelar()`, `Cliente.abonarSaldo()` | Test unitario: cancelar cambia estado + retorna monto; abonarSaldo suma correctamente |
| **H2** — Puerto y adaptador de búsqueda | `PuertoRepositorioSuscripcion.buscarPorId()`, `AdaptadorSuscripcionEnMemoria.buscarPorId()` | Test unitario: buscarPorId retorna Optional correcto |
| **H3** — Servicio de dominio | `IServicioSuscripcion.cancelar()`, `ServicioSuscripcion.cancelar()` | Test unitario: orquestación completa (buscar, validar aislamiento, cancelar, abonar, guardar) |
| **H4** — Handler y Comando | `CancelacionComando`, `CancelacionManejador` | Test unitario: delega a servicio y mapea resultado |
| **H5** — Endpoint REST | `ControladorSuscripcion` + `POST /api/suscripciones/{id}/cancelar` | Test E2E: cancelación exitosa, error si no activa, devolución de saldo |

### Decisiones Arquitectónicas

1. **`Suscripcion.cancelar()` retorna `BigDecimal monto`** — Encapsula validación de estado en dominio y expone monto para devolución.
2. **Reutilización de `FondoResultado`** — Campos idénticos (suscripcionId, clienteId, fondoId, monto, estado, fechaSuscripcion). No se crea `CancelacionResultado` separado.
3. **Nuevo `ControladorSuscripcion`** — Ruta `/api/suscripciones/{id}/cancelar` no encaja bajo `/api/fondos`. SRP: fondos vs suscripciones son recursos distintos.
4. **Parámetro `clienteId` en `cancelar()`** — Prepara aislamiento de datos (HU 1.3). Servicio valida que la suscripción pertenezca al cliente solicitante.

---

## Registro de Cambios

| Fecha | Versión | Descripción | Autor |
|-------|---------|-------------|-------|
| 2026-03-11 | 1.0 | Historia creada por PO | PO |
| 2026-03-12 | 1.1 | Análisis arquitectónico completado — 9 componentes, 5 hitos | Arquitecto |

---

## Metadata

**Autor:** PO  
**Fecha:** 2026-03-11  
**Historias relacionadas:** HU 2.1, HU 2.3, HU 1.3

### Trazabilidad de Slicing

- **Historia origen:** Gestión de suscripciones a fondos de inversión (suscripción, cancelación, listado, notificación)
- **Historias hermanas:** HU 2.1 (Suscripción), HU 2.2 (Cancelación), HU 2.3 (Consulta vigentes), HU 2.4 (Notificación)
- **Orden de implementación:** HU 2.1 (base) → **HU 2.2** (paralela con HU 2.3) → HU 2.4
- **Slicing justificación:** La cancelación es un flujo inverso a la suscripción con sus propias validaciones, cambio de estado y devolución de saldo. Tiene criterios de aceptación propios y es testeable de forma independiente.

---

## Métricas de redacción de la historia de usuario con el PO

**Tiempo con Método Ceiba:** 8 minutos  
**Tiempo estimado tradicional:** 22 minutos  
**Optimización:** 64%

---
